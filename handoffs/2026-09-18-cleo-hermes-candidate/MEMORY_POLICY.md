# Memory policy for the candidate

- Start with the compact USER seed only. Do not copy the current Hermes USER.md or MEMORY.md, the OpenClaw memory store, Vault, or transcripts.
- Keep MEMORY.md initially empty. Add only durable, verified operational facts or interaction preferences that improve a later session; attach date and provenance in the entry. Do not save an emotional moment or a biographical inference as a standing trait.
- Enable the documented `memory.write_approval: true` in the isolated candidate when Andrea authorizes installation. Review staged built-in memory writes with `/memory pending`; approve only concise entries. This is a proposed candidate-only policy, not a current setting.
- Respect the installed limits: USER.md 1,375 characters; MEMORY.md 2,200 characters. Aim below half of each cap. The snapshot is injected at session start; use a new session boundary to evaluate a memory change.
- Honcho is configured as the current external memory provider. The built-in `memory.write_approval` gate does **not** by itself govern Honcho's automatic message writes. Before candidate use, verify its effective workspace/peer mapping and `saveMessages` setting; use a separate candidate workspace with automatic message saving off for initial evaluation unless Andrea explicitly opts in. Do not import the default profile's history.
